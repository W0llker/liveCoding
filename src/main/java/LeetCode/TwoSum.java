package LeetCode;
//Имея массив целых чисел и целое число , возвращают индексы двух чисел так, что они складываются в целевую цель numstarget
//Можно предположить, что каждый вход будет иметь ровно одно решение, и не использовать один и тот же элемент дважды.
//Вы можете вернуть ответ в любом порядке.

//Пример 1:
//Input: nums = [2,7,11,15], target = 9
//Output: [0,1]
//Explanation: Because nums[0] + nums[1] == 9, we return [0, 1].
//Пример 2:

public class TwoSum {

    static void main() {
        twoSum(new int[]{1,4,5,8},9);
    }

    static void twoSum(int[] nums, int target) {
    }
}
